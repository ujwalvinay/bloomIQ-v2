export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>BloomIQ</h1>
      <p>API routes live under <code>/api/*</code>. See <code>docs/api.md</code>.</p>
    </main>
  );
}
